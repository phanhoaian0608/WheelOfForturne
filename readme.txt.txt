Install :
- You need to install Flask, Pusher and activate virtualenv to run code.
- Use $flask run in terminal or run it directly. This code will host on localhost, port 5000.
- Click on the name in the online players list to request a new game.